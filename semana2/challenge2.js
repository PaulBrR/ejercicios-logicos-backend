function divisibleByLeft(n) {
    //Implementación
    const numbers = n.splice("");
    console.log(numbers);

    for (let i = 0; i < numbers.length; i++) {;

        const element = numbers[i];
        
    }
}

module.exports = divisibleByLeft;